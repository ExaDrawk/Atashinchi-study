name = "study-app-api"
main = "src/index.js"
compatibility_date = "2024-01-01"

# R2バケット設定
[[r2_buckets]]
binding = "BUCKET"
bucket_name = "study-app-data"

[vars]
# API認証キー（Render.comからの呼び出し時に使用）
# 実際の値はwrangler secretで設定
# API_SECRET = "your-secret-key"
